/**
 * Protocol adapter base interface
 * Ported from Chat2API protocols/base.ts
 */

/**
 * @typedef {Object} ToolProtocolDetection
 * @property {boolean} matched
 * @property {boolean} partial
 * @property {number} [markerStart]
 */

export default {}
